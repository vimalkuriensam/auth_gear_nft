const utilsReducerDefaultState = {
  loader: false,
};

const utilsReducer = (state = utilsReducerDefaultState, { type }) => {
  switch (type) {
    default:
      return state;
  }
};

export default utilsReducer;
